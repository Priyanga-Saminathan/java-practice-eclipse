package Myclass;
import java.util.*;
public class concurrentchar {
	public static void main(String args[]) {
		Scanner sc=new Scanner(System.in);
		String str=sc.next();
		char arr[]=str.toLowerCase().toCharArray();
		StringBuilder sb=new StringBuilder();
		char pre=0;
		int sin=0;
		int ein=arr.length;
		for(int i=sin;i<ein;i++) {
			if(arr[i]==arr[i+1]) {
				sb.de
			}
		}
	}
}
